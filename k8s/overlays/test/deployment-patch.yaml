apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-watcher
spec:
  template:
    spec:
      containers:
        - name: pod-watcher
          image: mesut4730/go:v8
          imagePullPolicy: Always
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
